To get the Jekyll website running locally, execute the command
# bundle exec jekyll serve
To update the website if the config file changed, execute
# bundle update

Documentation:
github.com/sfarrens/jekyll_tutorial
